name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@v1
      with:
        submodules: true
    - name: build
      run: install/mac_build
    - name: test
      run: install/mac_tests

